var React = require('react');

var PageNotFound = React.createClass({
    render: function () {
        return (
            <h1>
                Page not found
            </h1>
        );
    }
});

export default PageNotFound;
